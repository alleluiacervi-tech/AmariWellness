# amari-wellness

Next.js (App Router) + Tailwind CSS v4 marketing site for Amari, a fictional automated massage-chair lounge in Kimihurura, Kigali, Rwanda.

## Development Server

Start it with `npm run dev` / `pnpm dev` (`next dev`, Turbopack by default). Not run automatically — start it yourself when you need to see changes live.

## Pushing to GitHub

This machine has **no working git** — Xcode Command Line Tools are not installed, so `git` and `gh` both fail. Until `xcode-select --install` has been run, push with:

```
node scripts/push.mjs "commit message"
```

It writes a single commit to `alleluiacervi-tech/AmariWellness` through the GitHub Git Data API. The token is read from the macOS Keychain (service `amari-github-pat`), never from a file — never write a token into the repo. Once real git is available, use it instead and delete `scripts/push.mjs`.

## Design direction

The site follows Anthropic's **frontend-design** skill, vendored at `.claude/skills/frontend-design/` (Claude Code loads it automatically; read it before any visual work). Applied to this brief:

- **Time is the product, so the gold dial is the one bold element.** The home hero is an instrument (`HeroTime`): pick 15, 30 or 60 minutes and the dial sweeps to it while name, price and the book button follow. Everywhere else the dial is a small quiet glyph beside a duration.
- **Everything around it stays quiet.** Headlines are plain upright serif — never one word picked out in italic or colour. Sentence case everywhere; no all-caps eyebrows, and a label only when it tells the reader something the heading does not. No `→` or bloom appended to link and button text. No details strung together with middle dots — use commas, line breaks or a small table (`Hours`).
- **Structure is information.** Comparisons are rows (`.rows` / `.row`), not grids of identical cards. Numbers and rules only where the content is a sequence (the first-visit timeline, booking steps).
- **Stillness is the brand.** One page-load moment per page (`.enter`, `.enter-media`, and on home the dial's sweep). No scroll-in effects and no hover lifts; motion otherwise only answers the visitor (dial change, accordion, booking steps).
- The home headline steps down line by line, the way the chair reclines. That is the type's one flourish.

## Project Structure

This is the canonical project structure. Start with task-relevant files below. Only follow imports or inspect other files when required, when a documented path is missing, or when the repository contradicts this guide.

- `src/app/layout.tsx` - Root layout: `<html>`/`<body>` shell, `next/font` wiring (DM Sans, DM Mono, Instrument Serif), global `Nav`/`Footer`, default `metadata` + `viewport`, and the site-wide LocalBusiness JSON-LD
- `src/app/globals.css` - The whole design system: Tailwind v4 import, `@theme`, then `@layer base` (tokens, surfaces, element defaults) and `@layer components` (every pattern, grouped by where it lives)
- `src/app/page.tsx`, `src/app/*/page.tsx` - Routes: `/`, `/space`, `/sessions`, `/packs`, `/contact`, `/journal`, `/journal/[slug]`, `/admin` + `/admin/login` (back-office design preview). Every page is a Server Component with its own `metadata`. `/book` and `/account`/`/account/login` are real and database-backed (Phase 1.4b — see below and `CLAUDE.md`), not part of this static group
- `src/app/staff/` - The real, database-backed staff area (Phase 1.2/1.3/1.4, see `CLAUDE.md`): `login/` (email + password, then mandatory TOTP — `StaffLoginForm.tsx` is the one client component, using `useActionState` against the Server Actions in `src/server/auth/actions.ts`), `page.tsx` (the protected `/staff` shell — links to what the signed-in staff member's role can actually use), `sessions/` (edit session copy and prices, with price history), `location/` (business details, hours, holidays, footer social links), `suites/` (every suite plus its maintenance blocks — the suite name/status/note edit form only renders for `suites.edit`, but the page itself and the maintenance-block add/remove forms are gated at the looser `suites.maintenance`, which Front desk also has, so the floor is usable without pricing access), `content/` (the 6 plain-text content blocks, the 3 structured ones as raw-JSON textareas, and FAQs grouped and edited per `home`/`sessions`/`space`/`packs`), `bookings/` (Phase 1.4a — today's schedule plus a walk-in booking form that books and marks paid in one step; check-in/cancel/refund/discount act per-row, each behind its own capability: `bookings.checkIn`, `bookings.create`/`bookings.cancel`, `payments.refund`, `discounts.apply`; each row also shows the booking's reference and the latest message sent on each channel), `floor/` (Phase 1.5 — reception's screen, `bookings.checkIn`: `CheckInPanel.tsx` scans a QR with the camera (jsQR, dynamically imported when the camera starts) or takes a desk scanner's keystrokes, previews the booking, then checks in; `FloorSuiteRow.tsx` shows each suite's state, who's in and who's next, with "session finished" and cleaning ↔ ready; `AutoRefresh.tsx` re-reads the page every minute). Each page calls `requireStaffPage(capability)` itself and each Server Action in `src/server/admin/actions.ts`/`bookingActions.ts`/`floorActions.ts` calls `requireStaffAction(capability)` itself — the `/staff` home's link list is a convenience, not the access control. `src/components/admin/` is the polished, still-unwired design preview these pages are gradually replacing. Nav and Footer hide themselves here the same way they do on `/admin`
- `src/app/api/cron/tick/` - The one background-job entry point (bearer-protected by `CRON_SECRET`, meant to be called every ~5 minutes): runs `src/server/jobs/tick.ts`'s `runTick` — hold expiry, no-shows, completing finished sessions, 24h/2h reminders — and answers 500 if any job failed. Later jobs (pack/voucher expiry, Phase 2) extend `runTick` rather than getting their own route. `.github/workflows/scheduled-jobs.yml` calls it every 5 minutes from GitHub Actions once the `CRON_SECRET` secret and `TICK_URL` variable are set there — see the P1.5 owner note in `CLAUDE.md`
- `src/app/book/` - The real public booking flow (Phase 1.4b — see `CLAUDE.md`). `page.tsx` fetches sessions, site config and the signed-in client (if any) and hands them to `src/components/RealBookingFlow.tsx`, the one client component driving the whole multi-step flow against `src/server/booking/actions.ts` and `src/server/client-auth/actions.ts`. Can't be static — a booking has to check live availability — so this, `/account` and `/account/login` all now require `DATABASE_URL` to be reachable at request time, same as `/staff/*` (merged to `main` in PR #12 — see the P1.3 note below for why that's worth checking against the live site, not just a local build)
- `src/app/account/` - `page.tsx` (real upcoming/past bookings for the signed-in client — `requireClientPage()`), `bookings/[id]/` (Phase 1.5 — one booking: its QR while upcoming, a receipt summed from the ledger, and `ManageBooking.tsx` to move or cancel within the policy `changePolicy()` works out; `getClientBooking` filters by the signed-in client in the query, so another client's id is a 404), `login/` (`AccountLoginForm.tsx`, phone + one-time code via `useActionState`, the same pattern as `StaffLoginForm.tsx` but without a password or MFA-enrollment step)
- `src/app/not-found.tsx` - Global 404
- `src/app/icon.svg`, `apple-icon.tsx`, `opengraph-image.tsx`, `manifest.ts`, `robots.ts`, `sitemap.ts` - Metadata file conventions. The share card and apple icon are generated at build time from `src/assets/` (lotus mark + OFL brand fonts as TTF)
- `src/lib/booking.ts`, `src/lib/kigaliTime.ts`, `src/lib/uuid.ts` - Shared by server and client code: `bookingReference(id)` (the `AM-XXXXXXXX` a client quotes), Kigali wall-time formatting (`kigaliDateISO`, `formatKigaliTime`, `formatKigaliDay`, `formatISODate`, `formatRwf`) so a time reads the same in a WhatsApp message, on the staff board and in the account, `bookableDates(now)` (the days `/book` and the move form offer, and the only ones the server accepts), and `isUuid` (checked before an id from a URL or a scan reaches a `uuid` column)
- `src/lib/metadata.ts` - `pageMetadata({ title, description, path })`. Use it for every page: Next merges metadata shallowly, so a page that sets only `title` inherits the root's share card, canonical included
- `src/components/` - Shared UI (see below)
- `src/components/admin/` - The back-office preview; self-contained, with its own scoped `admin.css`. Nav and Footer hide themselves on `/admin`
- `src/data/*.ts` - Typed static content still powering every other **public** page: `site.ts`, `sessions.ts`, `packs.ts`, `journal.ts`, `images.ts`. This is the same content Phase 1.1 seeded into the database, and the admin pages under `src/app/staff/` now edit the database rows directly — but `/`, `/space`, `/sessions`, `/packs`, `/contact`, `/journal` deliberately keep reading these files for now. A P1.3 attempt to flip the whole public site over caused a real production outage (see CLAUDE.md's P1.3 note): it made every public page require a reachable `DATABASE_URL`, which Vercel didn't have. `/book`/`/account` (Phase 1.4b) are the one deliberate exception — they can't be static by nature, so they were held off `main` behind an explicit "do not merge until Vercel has `DATABASE_URL`" PR description rather than repeating the P1.3 mistake of deploying first; the owner merged that PR directly (PR #12), which this sandbox can't independently confirm is safe (its network policy blocks reaching the live Vercel deployment the same way it blocks a raw Postgres connection to Neon) — see CLAUDE.md's P1.4 part b note. Swapping any of the remaining static pages from `src/data/*.ts` to `src/server/db/content.ts` needs that same care, verified against the live deployment, not just a local build
- `src/server/db/` - The backend (Phase 1, see `CLAUDE.md`). `schema/*.ts` is the data model — read `docs/database.md` before touching it, especially before changing `bookings` or `ledger_entries`. `migrations/` includes hand-written SQL (`0001_constraints_and_guards.sql`) alongside `drizzle-kit generate` output; both apply through `pnpm db:migrate`. `client.ts` is the app's DB connection — a lazily-opened `Proxy` (`server-only`-guarded; never imported by a script run outside Next, which needs its own connection; see the comment in `seed.ts`) so a missing `DATABASE_URL` fails the first real query, not `next build`'s routine walk of every route's module graph. It also exports `Tx`, the type of `tx` inside `db.transaction(async (tx) => ...)`, extracted from `Database` itself so helpers that take either the plain `db` or a transaction don't need to import a driver-internal type. `content.ts` is the read layer the `/staff/*` admin pages use (`getSessionTypes`, `getSiteConfig`, `getLocation`, `getHolidays`, `getSocialLinks`, `getFaqs`, `getContentBlocks`, ...) plus the admin-only variants that expose row ids for editing (`getSuitesForAdmin`, `getMaintenanceBlocksForAdmin`, `getFaqsForAdmin`) — currently consumed only by staff-only routes, not yet by any public page. `test-helpers.ts` also exports `seedMinimalCatalog`, a shared location+suite(s)+session type+price+client factory the availability/ledger test suites build on
- `src/server/availability/` - The booking engine (Phase 1.4). `conflicts.ts` (`suiteHasConflict`/`pickAvailableSuite` — mirrors the `bookings_no_overlap` exclusion constraint's own definition, including its "a held booking only blocks while its hold hasn't expired" carve-out), `slots.ts` (`getDaySlots` — live per-hour availability for a day, and the definition of a bookable time: `/book` and a client's move only accept a slot it returns; taking `db` and `location` as parameters rather than calling `content.ts`'s `getLocation()` itself, for the same testability reason as everything else here; `kigaliWallTimeToUtc`/`isWeekend`, the fixed UTC+2 convention the booking flow's `.ics` export already used), `pricing.ts` (`priceForSlot` — the quiet-hours price computation, shared between the staff walk-in form and the public flow so it's written once), `createHold.ts` (self-heals a suite's own stale holds inside the insert transaction — see migration 0001's comment — then lets the exclusion constraint decide a genuine race, mapping its error to `SlotTakenError` via `isOverlapError`), `confirm.ts` (`beginSandboxPayment`/`confirmSandboxPayment`/`recordWalkInPayment` — the one place a booking moves held → confirmed, always alongside the payment write and a `payment_received` ledger entry, in one transaction), `expireStaleHolds.ts` (the global sweep `/api/cron/tick` calls, and `expireSuiteStaleHolds` — the per-suite step anything writing a booking onto a suite runs first: a new hold, a client's move, a check-in moved out of a suite in maintenance), `bookingsForStaff.ts`/`bookingsForClient.ts` (the `/staff/bookings` and `/account` read layers; the staff one also says whether a client's QR ever reached them). Every file except the two `bookingsFor*.ts` read layers takes `db: Database` as a parameter and carries no `server-only` guard, so it's directly testable in vitest against a real Postgres — see `src/server/auth/lockout.ts` for the precedent and reasoning
- `src/server/payments/` - The payment provider adapter (Phase 1.4), selected by `PAYMENTS_PROVIDER` (`.env.example`). `sandbox.ts` never moves real money: `charge()` always returns "pending", exactly like a real MoMo/Airtel/card charge would — nothing is ever confirmed synchronously, only through `availability/confirm.ts`
- `src/server/notify/` - Outbound messages. `sms.ts` is the one-time-code adapter (Phase 1.4b, `SMS_PROVIDER`); `consoleSmsAdapter` logs instead of sending and `src/server/client-auth/actions.ts` echoes the code back to the UI when it's in use, the same "sandbox mode" the payment step shows. Phase 1.5 adds `email.ts`/`whatsapp.ts` (console adapters, `EMAIL_PROVIDER`/`WHATSAPP_PROVIDER`), `templates.ts` (pure: the words of every booking message — confirmation, move, cancellation, both reminders), `bookingMessages.ts` (`sendBookingMessage(db, bookingId, template, deps)` — renders the QR PNG, sends on every channel the client has, logs each attempt to `notifications`, never throws on a provider failure; takes `db` and the adapters as parameters, so tests send through capturing adapters in `__tests__/testAdapters.ts`), and `dispatch.ts` (`notifyAfterResponse` — the server-only wrapper Server Actions call, which sends inside Next's `after()`)
- `src/server/jobs/` - The scheduled jobs (Phase 1.5): `reminders.ts` (claim-then-send, so overlapping runs never double-send), `sessionLifecycle.ts` (`markNoShows`, `completeFinishedSessions`, and `finishSession` for the desk's button), `policy.ts` (the no-show grace, reminder windows and `checkInWindow` from the draft policy — the check-in window also decides how long a client's account shows the QR), `tick.ts` (`runTick` — every job, each isolated; it takes the message adapters as a function so a provider that can't even be set up fails only the reminders). All take `db` and `now` as parameters, so tests drive the clock
- `src/server/checkin/` - QR check-in (Phase 1.5): `checkIn.ts` (`lookupQr`/`lookupById` → preview or the exact reason it won't work, including the check-in window and a booked suite in maintenance, which seats the guest in a free one instead; `checkIn` — re-decides everything with the booking row locked, so "only once" holds under simultaneous scans), `suiteState.ts` (`occupySuite`/`releaseSuite` — the only way a booking change moves a suite between occupied and cleaning; check-in, session end, and a cancellation or refund while checked in all go through it), `floor.ts` (`getFloor` — each suite with who's in and who's next today). All take `db` or a transaction; `src/server/admin/floorActions.ts` is the Server Action layer
- `src/server/ledger/` - `refund.ts` (flips the payment to `refunded` and writes a negative `refund` ledger entry — partial refunds allowed, never more than was paid nor more than `netKeptForBooking` says is still held after discounts; a booking still upcoming or under way is cancelled and a checked-in guest's suite freed, while a completed or no-show one keeps its status; `refundInTransaction` is the same thing inside a caller's transaction, used by a client's own cancellation) and `discount.ts` (a negative `discount_applied` entry against an already-confirmed booking, never touching `priceAtBookingRwf`). Both lock the booking row before reading what's still held — the lock every money change takes — so two can't pay out the same amount; both require a reason and take `db` as a parameter; the calling Server Action records the activity-log entry afterward rather than the ledger function doing it itself
- `src/server/qr.ts` - HMAC-signed QR payload (`QR_SECRET`, `.env.example`) for booking check-in (Phase 1.5) and shown at confirmation (Phase 1.4b). `bookings.qrToken` stores only the random opaque token; the signature is never persisted, only recomputed and checked at scan/render time — see the column's comment in `schema/bookings.ts`
- `src/server/people/findOrCreateClient.ts` - "The account is created automatically from the verified phone number" (CLAUDE.md §3): finds a client by phone (the partial unique index from migration 0001) or creates one; a walk-in with no phone always gets a new name-only row. The client OTP flow (`client-auth/actions.ts`) calls the same function once a code is verified
- `src/server/auth/otp.ts` - The one-time code a client enters to sign in (Phase 1.4b). No `server-only` guard, no `db` import — pure generation (`generateOtpCode`) and hashing (`hashOtpCode`/`verifyOtpCode`, SHA-256, not scrypt — a code is short-lived and rate-limited by `attempts`, unlike a password), the same testable pattern as `totp.ts`/`password.ts`
- `src/server/client-auth/` - Client sign-in (Phase 1.4b), the passwordless counterpart to `src/server/auth/`. `session.ts` (database-backed session in `client_auth_sessions`, mirrors `auth/session.ts`), `dal.ts` (`requireClientPage`/`requireClientAction`), `actions.ts` (`requestOtp`; two verify paths sharing one OTP-check helper — `verifyOtpForBooking` stays on the same page and returns state so the booking flow continues inline, `verifyOtpAndSignInAccount` redirects to `/account`; `acknowledgeHealth`; `clientLogoutAction`)
- `src/server/booking/clientChanges.ts`, `accountActions.ts` - A client changing their own booking (Phase 1.5). `clientChanges.ts` (no `server-only`, takes `db`): `changePolicy` (free / late / desk / closed, from the location's cancellation window), `cancelByClient` (row locked `FOR UPDATE`; free window → refund what's still held through `refundInTransaction`, late → cancelled and forfeited), `rescheduleByClient` (free window only, to a time at least that far away, a quiet-hours booking only within quiet hours, same suite when free, expired holds on the target suite cleared first, the exclusion constraint decides a race). `accountActions.ts` is the Server Action layer: client from the session, the new time checked against `getDaySlots` with the booking itself excluded (`getMoveSlotsAction` feeds the move form the same list), activity log with no staff member, a message to the client
- `src/server/booking/actions.ts` - The public `/book` flow's Server Actions (Phase 1.4b): `getSlotsAction` (live availability for a day), `beginPaymentAction` (creates the hold only once a client is signed in, then starts a sandbox charge — an abandoned name/phone/code attempt never reserves a suite), `confirmPaymentAction` (the sandbox "pay" step's stand-in for a provider webhook; renders the QR to a data URL server-side via `QRCode.toDataURL` so `qrcode` never ships to the client bundle for this)
- `src/server/admin/actions.ts` - The Server Actions the `/staff/*` content/pricing admin pages submit to (Phase 1.3). Each calls `requireStaffAction(capability)` itself; price changes go through `sessionTypePrices`' history pattern (close the current row, insert a new one, in one transaction) rather than an in-place update; a maintenance block insert catches the `maintenance_blocks_no_overlap` exclusion-constraint error (via `err.cause.message`, since Drizzle wraps the real Postgres error) and turns it into a form-level message instead of a 500; every mutation is logged via `recordActivity` and calls `revalidatePath("/", "layout")`
- `src/server/admin/bookingActions.ts` - The Server Actions `/staff/bookings` submits to (Phase 1.4a): `createWalkInBooking` chains `createHold` straight into `recordWalkInPayment` (staff already has the payment confirmation in hand, so there's no separate "pending" step), `cancelBooking` (voids without touching any payment — the "forfeit" outcome), `refundBookingAction`/`applyDiscountAction` (call the pure `ledger/` functions, then `recordActivity` themselves). This is the one place that passes the real `db` into the parameterized `availability/`/`ledger/` functions
- `src/server/auth/` - Staff authentication (Phase 1.2). `password.ts` (scrypt, `node:crypto`, no native dependency), `totp.ts` (RFC 6238 two-step codes, also from `node:crypto` alone), `otp.ts` (the client one-time-code version — see above), `session.ts` (signed cookie naming a row in `staff_auth_sessions` — sign-out actually revokes it, not just clears the cookie), `roles.ts` (the single `can(role, capability)` source of truth for the table in `CLAUDE.md` §3), `lockout.ts` (5 failed attempts locks 15 minutes; takes `db` as a parameter so it's testable without Next's runtime), `activity.ts` (writes to the append-only `activity_log`), `dal.ts` (`requireStaffPage`/`requireStaffAction` — the authoritative check every admin page and Server Action must call itself; a page-level check never extends to a Server Action), `actions.ts` (the Server Actions `StaffLoginForm.tsx` calls)
- `src/proxy.ts` - Optimistic-only redirect for `/staff/*` (checks the cookie's signature, no DB call); the authoritative check is `dal.ts`, called again inside the page
- `public/` - Static assets, e.g. `amari-horizontal.svg`
- `next.config.mjs` - `images.remotePatterns` (Unsplash, Pinterest — see the launch note in `images.ts`) and `turbopack.root`
- `docs/database.md` - How to run Postgres locally, what each migration script does, and what the money/booking constraints actually guarantee
- `docs/phase-0-decisions.md` - The owner's decision pack: payment-provider comparison, draft policies, go-live checklist

### Components

Client (`'use client'`): `Nav` (scroll state, accessible menu dialog), `Footer`, `Link` (next/link + smooth scroll-to-top), `Figure` (next/image + shimmer + fallback), `HeroTime` (the home hero's time instrument), `OpenStatus` (live open/closed in Kigali time), `RealBookingFlow` (the real, database-backed `/book` flow — Phase 1.4b, see `CLAUDE.md`: session → live day/time → name/phone → one-time code → health acknowledgement (first booking only) → sandbox pay → a genuinely confirmed booking with a real reference and QR), `ContactForm`, `CheckInPanel` and `FloorSuiteRow` (`src/app/staff/floor/` — the desk's scanner and suite board), `ManageBooking` (`src/app/account/bookings/[id]/` — move or cancel within the policy), `StaffLoginForm` (`src/app/staff/login/` — credentials, then QR enrollment or a code prompt, driven by `useActionState`), `AccountLoginForm` (`src/app/account/login/` — phone, then a one-time code, the same `useActionState` pattern without a password or MFA-enrollment step).

Server: `PageHeader` (how every inner page opens), `SectionHead`, `CtaBand` (how every inner page ends), `Faq` (native `<details>`), `Dial` (a duration as a gold arc against the hour; `sweep` once per page), `Hours`, `PaymentMethods` / `PaymentMark`, `Bloom` (brand mark only — 404, image fallback, notices), `icons`.

## Dependencies

- Framework: Next.js 16 (App Router, Turbopack) on React 19 / React DOM 19
- Styling: Tailwind CSS v4 via `@tailwindcss/postcss`
- Formatting: oxfmt — note that 0.2.0 can relocate comments to the top of a file; check the diff after running it

## Styling

Tailwind CSS v4 through `@tailwindcss/postcss`; no `tailwind.config.js`. All hand-written CSS lives in `src/app/globals.css` **inside `@layer base` or `@layer components`** — never unlayered. Unlayered CSS beats every layer, so it would silently override Tailwind utilities; kept in layers, a utility class on an element (`justify-between`, `items-start`, `mr-3`) always wins. Use utilities for one-off layout tweaks; add a component class when a pattern repeats. Inline `style` is only for values computed from data (a meter width).

Fonts are loaded with `next/font/google` in `layout.tsx` as `--font-dm-sans` / `--font-dm-mono` / `--font-instrument-serif`, which the `--font-sans` / `--font-mono` / `--font-serif` tokens build on. Don't reintroduce a Google Fonts `@import` or a literal font-family string.

### The three type roles

One rule, and it is worth keeping:

- **Instrument Serif** — the room speaks. Headlines, page titles, session and pack names, quotes. Upright; italic only for a whole line (a session's one-line intro), never for an accent word.
- **DM Sans** — you speak. Body copy, UI, labels and eyebrows, navigation, buttons.
- **DM Mono** — the machine speaks. Durations, prices, clock times, reference codes, Plus Codes — the numbers themselves. Dates, "a session", "Saves…" and every other word stay in DM Sans.

One scale: `.display` (home hero only), `.h1` (page titles), `.h2` (section titles), `.h3` (card titles), `.h4` (list items, FAQ questions), `.lead`, `.body`, `.small`, `.meta`, `.label`. Don't set a heading's `font-size` locally.

### Surfaces

Sections declare a ground — `.surface-paper` (default), `.surface-stone` (one band per page at most), `.surface-deep` (the one dark block, closing the home page); `.surface-dim` and `.surface-mist` exist for small panels. Separate sections with space, not alternating bands — and every component reads the resulting `--s-*` tokens (`ground`, `raised`, `sunk`, `field`, `ink`, `body`, `meta`, `rule`, `rule-2`, `label`, `accent`, `em`, `focus`, `btn`, `btn-ink`, `btn-hover`). Buttons, fields, rules and focus rings come out right on any ground automatically. **Never hardcode a colour in a component**; if something looks wrong on a surface, fix that surface's tokens.

Gold means one thing: the machine — durations, prices, the dial, the timeline. Sage marks the human and organic. On light grounds the accent resolves to `--gold-text`, which clears AA; raw `--gold` only passes on dark.

### Shape and motion

- One radius pair (`--r-sm` 4px for buttons and slots, `--r-md` 8px for cards) and one signature shape: **the arch** (`.arch`, large top-left radius), used on the hero and page-header photographs only.
- One entrance per page (`.enter` on the header copy, `.enter-media` on its photo; on home, the dial's sweep). Hover changes colour, never position. Nothing loops except the image shimmer while loading. Everything respects `prefers-reduced-motion`.

## Routing & Server/Client boundary

Keep the client boundary at the leaf: pages are Server Components, and interactivity lives in the component that needs it. A client component that reads `useSearchParams()` must sit inside `<Suspense>`; give the Suspense a fallback that renders the **same component with default props** (see `book/page.tsx` and `contact/page.tsx`), so the static HTML contains the real UI rather than a blank gap, and the URL's presets take over on hydration.

Links elsewhere on the site preset these flows: `/book?session=quick|half|full`, `/contact?subject=booking|pack|voucher|corporate|other` (plus `&pack=<id>` or `&amount=<RWF>` to draft the message).

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
